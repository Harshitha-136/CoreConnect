# CoreConnect
Secure LAN Messenger is a real-time web-based chat application that enables instant text and audio communication within a local network using Socket.IO. Built with React and Node.js, it supports voice recording, file uploads, and live user tracking.
